apiVersion: v1
kind: ConfigMap
metadata:
  name: pdf-pipeline-config
  namespace: {{ include "pdf-pipeline.namespace" . }}
data:
  VLM_URL: {{ .Values.vlmUrl | quote }}
  TASKS_DB_PATH: "/data/tasks.db"
  OUTPUTS_ROOT: "/app/outputs"
